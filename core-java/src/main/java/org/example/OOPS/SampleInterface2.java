package org.example.OOPS;

/*
for interface can't create object.
so can be used in the inheritance as a parent interface with keyword implements
can declare the method & variables
should not have definition.
multiple inheritance can be allowed only for interface

 */
public interface SampleInterface2 {
    void printMessage2();//declaration.
}
